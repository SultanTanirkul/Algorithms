#pragma once

namespace c_Algorithms_DS {

    // Utility Function for swapping the two elements
    void swap(int* a, int* b);

	// Validation if two Arrays are equal
	bool checkIfEquals(int * A, int * B, int len);

}
